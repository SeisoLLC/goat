---
name: Seiso Grand Opinionated AutoTester (GOAT)
description: Apply Seiso's standard testing
inputs:
  disable_terrascan:
    description: 'github/super-linter terrascan environment variable'
    required: false
    default: 'false'
runs:
  using: 'docker'
  image: 'Dockerfile'
